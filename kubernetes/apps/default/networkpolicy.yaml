---
# Safety net: deny all ingress and egress for any pod in the default namespace
# that doesn't have its own NetworkPolicy explicitly allowing traffic.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
